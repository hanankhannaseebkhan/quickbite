name: React CI

on:
  push:
    branches: [main, Adwork, newbranch]
  pull_request:
    branches: [main, Adwork, newbranch]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: ⬇️ Checkout repository
      uses: actions/checkout@v3

    - name: 🟢 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 18

    - name: 📦 Install dependencies
      run: npm install

    - name: ✅ Run tests
      run: npm test --if-present

    - name: 🛠️ Build project
      run: npm run build
